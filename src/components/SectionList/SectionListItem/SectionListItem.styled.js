import styled from '@emotion/styled';

export const LiStyled = styled.li`
  a {
    color: var(--secondColor);
  }

  h3,
  #course {
    font-weight: 600;
  }
  
  #period {
    margin: 3px 0;
    font-size: 3.5mm;
  }
`;
